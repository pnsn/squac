import { OrganizationPipe } from './organization.pipe';

describe('OrganizationPipe', () => {
  it('create an instance', () => {
    const pipe = new OrganizationPipe();
    expect(pipe).toBeTruthy();
  });
});
